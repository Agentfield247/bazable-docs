
#### `docs/commands/docs.md`
```markdown
# bazable docs

Generates static API documentation HTML from the contract.

## Usage
```bash
bazable docs [options]

Options
-o, --output <dir> : Output directory (default ./bazable-docs)

## Example
bazable docs -o ./public/api-docs
